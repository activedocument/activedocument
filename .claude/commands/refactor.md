---
description: Hard refactor (breaks backwards compatibility)
---

Unless otherwise indicated by "soft", this means a hard, aggressive refactor which does not preserve backwards compatibility and removes/cleanups and obsolete code paths.
