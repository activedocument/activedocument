# Mongoid 7.6-stable Branch Analysis

**Date:** 2026-02-01
**Branch:** `upstream/7.6-stable`
**Commits analyzed:** 5 (unique to 7.6-stable, not in 7.5-stable)

## Summary

All significant code changes from Mongoid 7.6-stable are present in ActiveDocument master. This was a small maintenance release with only 2 feature commits.

## Key Features Verified as Merged

| JIRA | Description | Status | Notes |
|------|-------------|--------|-------|
| MONGOID-5864 | Add Rails metadata | MERGED | `lib/active_document/clients/factory.rb` |
| MONGOID-5919 | Restrict to_criteria (Criteria.from_hash) | MERGED | `lib/active_document/criteria.rb` |

## Architecture Notes

### Rails Metadata (MONGOID-5864)
Adds Rails version to the `wrapping_libraries` metadata sent to MongoDB server:
```ruby
if defined?(::Rails) && ::Rails.respond_to?(:version)
  wrap << { name: 'Rails', version: ::Rails.version }
end
```

### Criteria.from_hash Security (MONGOID-5919)
Restricts `Criteria.from_hash` to a whitelist of 29 safe query-building methods:
- Prevents unsafe method calls like `create`, `build`, `find`, etc.
- Uses `public_send` instead of `__send__` for safer invocation
- Raises `ArgumentError` for non-whitelisted methods
- Method is deprecated with `ActiveDocument.deprecate(self, :from_hash)`

## Conclusion

All 2 significant commits from 7.6-stable are present in ActiveDocument master.
